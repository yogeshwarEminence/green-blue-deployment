# Copy this file to .env for LOCAL (non-Docker) development.
# Inside docker-compose, these same values are injected directly
# via the "environment:" section in docker-compose.yml instead.

PORT=4000
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=employee_db
