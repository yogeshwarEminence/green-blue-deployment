# Copy this file to .env for LOCAL (non-Docker) development with `npm run dev`.
# When building the Docker image, this value is instead passed as a
# build ARG from docker-compose.yml (see build.args.VITE_API_URL).

VITE_API_URL=http://localhost:4000
